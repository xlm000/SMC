# 滑模算法

## 使用方法
	任务循环前按alg_smc.h的smc_init_t的参数说明填写函数（基本都要填）。
	任务循环中调用smc_tick_calculate，填入参数按照函数说明。
	注意！：实际以差分代替微分，计算间隔小，初始所给参数也应较小（c,k,eps小于1试起）

## 优化方向
	同济大学所用为传统终端滑模，可优化为双指数滑模面；
	针对pitch轴重力补偿的建模完善。
	sig函数。
